In learn.py kann man in Zeile 54 ändenr welche Umgebung Treniert wird und die Umgebung bis an,
-was beobachtet wird
-welche Aktionen erlaubt sind
-wann Episode endet
-wie Reward berechnet wird
-was das Ziel ist

fort kann man außerdem das abbruchkriterium editieren

Für ein Custom Ziel kann man entweder eine eigene Umgebung neu mach oder eine bestehende ändern

ZUm Beispiel in HoverAviary.pc Line 51 kann man 
self.TARGET_POS = np.array([0,0,1]) frei ändern und neue Kriterien hinzufügen um das ziel zu bearbeiten.

man kann den aktuellen Dronen Standort mit state = self._getDroneStateVector(0) und damit dann das Reward ausrechnen




Nachdem die das lernen auf dem Enviroment dann abgeschlossen wurde sieht man einmal den besten lauf den das Pogramm hinbekommen hat und Daten zu wie die Dorohne Statistisch geflogen ist

Ein Fertigen Modell kann dann nochmal in einer SImulation getestet, werden wo man die umgebung nachmal ändern kann und sehen kann wie es sich verhält, aber in dieser Phase lernt es nicht mehr

Hinderniss sind Plazierbar
